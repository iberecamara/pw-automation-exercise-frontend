export interface ProductType {
    index?: number,
    name: string,
    price: number,
    category?: string,
    availability?: string,
    quantity?: number,
    totalPrice?: number,
    condition?: string,
    brand?: string
}

